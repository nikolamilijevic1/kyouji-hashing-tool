version: '3.8'

services:
  backend:
    build: ./backend
    container_name: forensic-backend
    ports:
      - "8000:8000"
    env_file:
      - .env
    restart: always

  frontend:
    build: ./frontend
    container_name: forensic-frontend
    ports:
      - "8501:8501"
    depends_on:
      - backend
    restart: always
