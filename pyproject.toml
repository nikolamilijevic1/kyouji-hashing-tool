[project]
name = "kyouji-hashing-tool"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = []

[tool.uv]
workspace = { members = ["src/frontend", "src/backend"] }

[dependency-groups]
dev = [
    "pytest>=8.3.0",
]

[tool.pytest.ini_options]
pythonpath = ["src"]

